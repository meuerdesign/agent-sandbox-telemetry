global:
  scrape_interval: 30s

# Metrics arrive via remote-write from the otel-collector
# (--web.enable-remote-write-receiver is set in docker-compose.yml).
# No scrape jobs needed.
scrape_configs: []
